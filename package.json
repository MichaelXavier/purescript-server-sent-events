{
  "name": "purescript-server-sent-events",
  "version": "0.2.1",
  "description": "A purescript interface to the Server-Sent Events API (SSE).",
  "main": "server.js",
  "directories": {
    "test": "test"
  },
  "scripts": {
    "build": "spago build",
    "example": "spago bundle-app --path examples/Main.purs --to output/index.js",
    "server": "node examples/server.js",
    "spago:install": "spago install"
  },
  "author": "Michael Xavier",
  "license": "MIT",
  "dependencies": {
    "spago": "0.19.0",
    "purescript": "^0.14.0"
  },
  "devDependencies": {
    "express": "^4.15.3",
    "express-sse": "^0.4.1",
    "body-parser": "^1.17.2"
  }
}
